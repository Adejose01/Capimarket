## 🗺️ Roadmap de Ejecución: Categorías Dinámicas + Tags & Refactorización de Interfaz

### **Fase 1: Configuración en PocketBase (Backend & Schema)**

1. **Colección `categories`:**

- Limpiar la lista para dejar únicamente las 6 a 8 Categorías Macro necesarias (_Teléfonos & Accesorios_, _Laptops & Computadoras_, _Audio & Sonido_, _Componentes & PC_, _Gaming_, _Gadgets & Smartwatches_, etc.).
- Revisar y ajustar campos necesarios (ej: `name`, `slug`, `icon` o `image`).

2. **Colección `products`:**

- Agregar el campo `tags` (tipo `json` para almacenar un array de strings `["gamer", "bluetooth"]`, o `text` delimitado por comas).
- Validar la relación directa o indirecta con `categories`.

---

### **Fase 2: Capa de Servicios & Limpieza (Frontend Async/Promises)**

3. **Crear/Refactorizar `CategoriesService.ts`:**

- Crear el módulo estático en `/lib/services/pb/` con tipado estricto en TypeScript.
- Implementar `getActiveCategories()` usando sintaxis exclusiva de promesas `.then() / .catch()`, filtrando dinámicamente solo aquellas categorías que posean productos vinculados.

4. **Sanear llamadas a la BD:**

- [x] Reemplazar cualquier llamada directa a PocketBase sobre categorías en vistas existentes para forzar el uso centralizado de `CategoriesService`.
- Implemetar los tags dentro de `ProductsService`

---

### **Fase 3: Panel del Vendedor (Seller Interface)**

5. **Módulo Completo del Vendedor (`/seller`):**

- **Creación / Edición de Productos:** Formulario adaptado con `<select>` para las 8 Categorías Macro y un input dinámico para los **tags** (sanitizados a minúsculas, sin espacios ni símbolos, máximo 3 a 5).
- **Administración de Productos:** Tabla/Grid para gestionar el inventario del usuario (editar, pausar, eliminar).
- **Configuración de la Tienda (BrandSettings):** Ajustes de perfil del comercio (verificación de Instagram sin `@`, WhatsApp con código de país, etc.).
- **Agregar una barra de busqueda**

---

### **Fase 4: Marketplace, Búsqueda y Frontend Dinámico**

6. **Sanear Búsqueda por Tags:**

- Ajustar las consultas en `MarketplaceService.ts` para que la barra de búsqueda y filtros globales busquen coincidencias tanto en el título del producto como en el campo `tags`.

7. **Refactorización Visual del Marketplace:**

- **CategoryBar:** Renderizado dinámico conectado a `CategoriesService` (las categorías vacías no se muestran).
- **Bento Grid / Layout Principal:** Sanear y estructurar la vista de la Home.

8. **Tarjeta de Visualización de Producto (ProductCard):**

- Corregir y pulir el componente visual de las cards de productos (precio, tags, imagen, badging de estado, link a WhatsApp/tienda).
