# =========================================================
# Frontend Configuration for Production
# =========================================================
# Nota: Este archivo ya no se usa directamente en producción.
# El frontend se maneja en nginx.conf como upstream "frontend_backend".
# Este archivo se mantiene por compatibilidad con configuraciones locales.
